% Áelou! Káevarju dúing?  This is my first package ever and if it is
% beneficial to you, then I am very happy, my frjénding.
%
% (c) Anastázius Kaejatí Darián
%
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{kedmaetan}
\RequirePackage{dialogue,lettrine,needspace,changepage,ifthen}

\@ifpackageloaded{xcolor}{\providecommand\ruh{\color{red}}}{}
\@ifpackageloaded{luacolor}{\providecommand\ruh{\color{red}}}{}
\providecommand\ruh{}

\@ifpackageloaded{ekdosis}{
  \newenvironment{enteqadi}{
    \setlength\linenumbersep{\maxdimen}\SetLineation{lineation=page,modulo,modulonum=100}
    \newenvironment{leftside}{\begin{edition*}}{\end{edition*}}
    \newenvironment{rightside}{\begin{translation}}{\end{translation}}
    \begin{alignment}
    }{
    \end{alignment}
  }
  \newcommand\resumeleftside{
    \begin{leftside}
    \end{leftside}
    \begin{rightside}
      \vbox to -\parskip {
        \faerman{
          --- Resume on the left page ---
        }
      }
    \end{rightside}
  }
}{}

\@ifpackageloaded{fancyhdr}{
  \pagestyle{fancy}
  \fancyhf{}
  \fancyhead[LE,RO]{\thepage}
  \renewcommand{\headrule}{
    \vspace{-2ex}
    \hbox to\headwidth{
      \ruh\leaders\hrule height \headrulewidth\hfill
    }
  }
}{}

\@ifpackageloaded{psaltiki}{
  \newcommand\mart[1]{\vbox to 0.7\baselineskip{\vfil\hbox to 1em{\hfill\ruh\neume #1\hfill}\vfil}}
}{}
\newcommand\daestur[1]{{\ruh\textsc{\MakeLowercase{#1}}}}
\newcommand\taemam[1]{{\setlength\parindent{0.5\parindent}\ \\\indent\indent\daestur{ #1: }\indent}}
\newcommand\doubare[1][2]{\daestur{(#1x)}}
\newcommand\faerman[1]{
  \Needspace*{5\baselineskip}
  {\setlength\parskip{0em}
  \linespread{0}
  \par\vspace{\baselineskip}
  \centering\daestur{#1}\par
  }\vspace{-\parskip}\nopagebreak
}
\newcommand\irmos[1]{\footnotesize(Irmos: \emph{#1})}
\newcommand\automelon[1]{\footnotesize(Automelon: \emph{#1})}
\def\drop #1#2 {
  \ifcat\noexpand\relax\noexpand#1\def\next{#1{#2}\drop}\else
  \def\next{\Needspace*{3\baselineskip}\lettrine{\ruh#1}{#2} }\fi
  \next
}
\newenvironment{haemsaefhe}{
  \noindent\begin{minipage}{\textwidth}
  }{
  \end{minipage}
}
\newcommand\gotoevenpage{
  \checkoddpage
  \ifthenelse{\boolean{oddpage}}{\hfill\newpage}{}
}
\newenvironment{goftegu}{
  \setlength\parskip{0.5em}
  \def\haerf##1{##1}
  \renewcommand\haerf[1]{\speak{\ruh ##1}}
  \begin{dialogue} }{
  \end{dialogue}
}
\newcounter{versenumber}
\newenvironment{chantverses}{
  \setcounter{versenumber}{0}
  \noindent\begin{minipage}{\textwidth}
    \def\chantverse##1{##1}
    \renewcommand\chantverse[1]{
      \refstepcounter{versenumber}
      \haerf{\nth{\theversenumber} Verse}\emph{##1}
    }
    \begin{goftegu}
      \setlength\parskip{0em}
    }{
    \end{goftegu}
  \end{minipage}
}
\newcommand\haerf[2]{
  \vspace{0.3\baselineskip}
  \Needspace*{4\baselineskip}
  \noindent\hspace{0em}\daestur{#1:}\hspace{0.5em}{\em #2}
  \nopagebreak
}
\newcommand\zirnaezd{
  \vspace{-\parskip}
}
\newcommand\zdrop{
  \zirnaezd\drop
}
